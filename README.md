# Archiving and Unpacking

## Archiving

To compile and run the `a.c` file using `heap.c` and `heap.h`, perform:

```bash
gcc a.c heap.c -o program
./program
```

## Unpacking

To unpack, run:

```bash
gcc b.c -o unarchive
./unarchive
```
